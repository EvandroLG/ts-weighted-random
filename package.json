{
  "name": "ts-weighted-random",
  "description": "Picks a random value from a list based on the weights",
  "version": "0.0.1",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.2.0",
    "husky": "^8.0.0",
    "jest": "^29.2.2",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "build": "tsc -d",
    "test": "jest",
    "prepare": "husky install"
  }
}
